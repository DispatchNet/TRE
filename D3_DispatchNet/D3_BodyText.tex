\section{Project Structure}
In the following is shown the structure of the project.

\imgins{images/structure/DispatchNet_project_structure.png}.

It is composed of: 

\begin{itemize}

    \item the main modules:
    \begin{itemize}
        \item departure: \textcolor{red}{TODO: ?};
        \item infrastructure: the network and its management;
        \item path\_finding: the algorithm to find a path between stations;
        \item service\_management: the management of services;
        \item user\_management: the management of users and their authentication.
    \end{itemize}

    \item secondary tools:
    \begin{itemize}
        \item csv\_database: the actual database and its management;
        \item IO\_operations: input/output operation to interact with the user from terminal;
        \item mail\_service: integration with a possible mail service, as well as a temporary mock one;
    \end{itemize}
    
\end{itemize}

\section{Architecture}
The project is intended, in its platonic ideal, as a Client-server structure, where the client shoulders only the user interface and requests to the server, who instead is responsible for the actualy interfacing with the database and computing the responses.

Thanks to our chosen \hyl{Implementation Language}, the Client may be nearly any device capable of general computing, and thus we want to minimize the client-side device requirements to fully exploit our great portability. 

Thus, the client is expected to perform only to the relatively light work of displaying responses and forming requests with user input to send the central server.

Not beholden to such limitations, the central server must then receive requests and perform the computatonally expensive operations, chief among them the weighted-graph-traversal over the train nework, but also loading and handling the information from the database.

Unfortunately, due to both skill and time limitations, the project is implemented as a temporary measure in a monolithic form, while still taking care to expect and enforce the necessary separation and operations for a hypothetical future division and upgrade to a client-server system ready for deployment.

\section{Implementation Language}\label{Implementation Language}
We chose \bf{Java} for the operational part and \bf{Doxygen} to generate documentation dynamically.


Our motivation for \bf{Java} was the extreme portability and the very module-friendly structure, allowing us to experiment and tinker with different implementations of the same expected behaviour in a plug-and-play fashion. 
The garbage collector feature of \bf{Java} also ensures us to spend more time working of the functioning of our systems rather than the management of their lifetimes.

\bf{Doxygen} was chosen due to its in-built support for \bf{Java} development and for its low overhead cost during code development, as the comment-tag system requires both very little time and brain-power to make, while still being very sight-readable in the code files.

\section{Project Dependencies}
\begin{itemize}
    \item JDK v25.0.3 or above
\end{itemize}

\section{Database}

To save data between different we used a database saved as several CSV files.\\
It will be loaded at startup, containing at least the account of the network manager, with default information.
The data will be managed within the code and saved to the database only at the end of the program.

The database contains one file for each set of data:
\begin{itemize}
    \item authenticated users
    \item junctions
    \item lines
    \item platform
    \item station data
    \item \textcolor{red}{TODO: add if necessary}
\end{itemize}

The authenticated user conatins the following fields:
\imgins{images/database/user_database.png}

The junction contains the following fields:
\imgins{images/database/junction_database.png}

The line contains the following fields:
\imgins{images/database/line_database.png}

The platform contains the following fields:
\imgins{images/database/platform_database.png}

The sstation data contains the following fields:
\imgins{images/database/stationdata_database.png}

\section{User Flows}

\osubs{User Management}
The following user diagram describes the actions related to the user management and user authentication, common to all the users.

It provides different pages depending on the type of user. An authenticated user (I.E. passenger or train company, not network manager) can delete, view data (Account Page), change data or logout (also the network manager).\\
An anonymous user can register, reset its password or login.

\imgins{user_diagrams/user_management_user.pdf}


\newpage
\osubs{Ticketing}
The following user diagram describes the flow of the ticketing component, started from the \hyl{Passenger}'s diagram.

\imgins{user_diagrams/ticketing_user.pdf}

\newpage
\osubs{Passenger}
The following user diagram describes the actions the passenger can take. It can go to \hyl{User Management}, view its ticket history or start a path finding procedure. In the last two it can move to the \hyl{Ticketing} flow.

\imgins{user_diagrams/passenger_user.pdf}

\newpage
\osubs{Train Company}
The following user diagram describes the actions the train company can take. It can go to \hyl{User Management} or to the service management.

\imgins{user_diagrams/train_company_user.pdf}

\newpage
\osubs{Network Manager}
The following user diagram describes the actions the network manager can take: managing the network and its elements.

\imgins{user_diagrams/network_manager_user.pdf}

\newpage

\section{APIs}
As this is a mock implementation, no real API has been implemented. Instead, we opted to build fake endpoints in the form of methods, so we could test the validity of our underlying data model.
All client-facing operations are found within the \verb|UserManagement| package, in the form of class methods grouped by their relevant user. 
The final implementation is planned to be a REST API and will adhere to the OpenAPI standard.

External APIs have been modeled as interfaces with mock implementations.
The interface paradigm allows us to use valid invocations towards a fake implementation that can easily be swapped at a later date by a real implementation.
Specifically, we have implemented the following fake external APIs
\begin{itemize}
    \item \verb|mail\_service|: The mail service
        \begin{itemize}
            \item Interface: \verb|MailService|
            \item Mock implementation: \verb|MockMailService|
        \end{itemize}
    \item \verb|payment\_gateway|: The payment gateway
        \begin{itemize}
            \item Interface: \verb|PaymentGateway|
            \item Mock implementation: \verb|MockPaymentGateway|
        \end{itemize} 
\end{itemize}
The mock implementation of payment gateway also has non-deterministic behavior allowing us to test adequate handling of real-world failure conditions.
\section{Code Preview}

\section{Team roles}

We divided the project into 3 natural modules, so that everyone worked on every aspect of software engineering flow. Following, specific \hyperref[Table1]{Roles and Activities}

\osubs{Roles and Activities}\label{Table1}
\begin{center}
	\begin{tabular}{c|c}
	\hline
	\bf{Team member} & \bf{Roles and Activities}\\
	\hline
	\begin{tabular}{@{}c@{}}
	Alessio Zanon
	\end{tabular} & Latex expert\\
	\hline
	\begin{tabular}{@{}c@{}}
	Leonardo Tonetta
	\end{tabular} & Team leader: monitoring deadlines, submitting deliverables, scheduling meeting\\
	\hline
	\begin{tabular}{@{}c@{}}
	Luca Fiorini
	\end{tabular} & Railway expert\\
\end{center}

\label{Table1: workload and distribution}
\begin{center}
	\begin{tabular}{c|c c c}
	\hline
	\bf{Team member} & \bf{D1} & \bf{D2} & \bf{D3}\\
	\hline
	\begin{tabular}{@{}c@{}}
	Alessio Zanon
	\end{tabular} & 34 & 33 & 33\\
	\hline
	\begin{tabular}{@{}c@{}}
	Leonardo Tonetta
	\end{tabular} & 33 & 34 & 33\\
	\hline
	\begin{tabular}{@{}c@{}}
	Luca Fiorini
	\end{tabular} & 33 & 33 & 34\\
\end{center}
